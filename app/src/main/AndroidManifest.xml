<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.focusstartrssreader">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".RssFeedApp"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:usesCleartextTraffic="true">
        <activity
            android:name=".UI.activities.AddNewFeedActivity"
            android:parentActivityName=".UI.activities.AddActivity"/>
        <activity
            android:name=".UI.activities.FeedDetailActivity"
            android:parentActivityName=".UI.activities.MainActivity" />
        <activity
            android:name=".UI.activities.AddActivity"
            android:label="@string/add_activity"
            android:parentActivityName=".UI.activities.MainActivity" />
        <activity
            android:name=".UI.activities.SettingsActivity"
            android:label="@string/settings_activity"
            android:parentActivityName=".UI.activities.MainActivity" />
        <activity android:name=".UI.activities.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <service
            android:name=".service.FetchFeedService"
            android:exported="false" />
    </application>

</manifest>