<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.focusstartrssreader">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".RssFeedApp"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:usesCleartextTraffic="true">
        <activity
            android:name=".UI.activities.add.AddNewFeedActivity"
            android:parentActivityName=".UI.activities.add.AddActivity"/>
        <activity
            android:name=".UI.activities.detail.FeedDetailActivity"
            android:parentActivityName=".UI.activities.main.MainActivity" />
        <activity
            android:name=".UI.activities.add.AddActivity"
            android:label="@string/add_activity"
            android:parentActivityName=".UI.activities.main.MainActivity" />
        <activity
            android:name=".UI.activities.settings.SettingsActivity"
            android:label="@string/settings_activity"
            android:parentActivityName=".UI.activities.main.MainActivity" />
        <activity android:name=".UI.activities.main.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <service
            android:name=".service.service.FetchFeedService"
            android:exported="false" />
    </application>

</manifest>